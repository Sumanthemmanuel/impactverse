# Core (required — pure logic, dedup, priority scoring, fairness)
numpy>=1.26
scikit-learn>=1.4

# Optional but recommended: zero-shot embedding domain classification.
# If this is missing OR the model weights can't be downloaded (no network,
# offline demo, etc.), the service automatically falls back to keyword
# matching — nothing breaks, `method` field just reports "keyword".
sentence-transformers>=2.2

# HTTP layer (optional — pipeline works as a plain library without these,
# main.py's `app` will just be None)
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6

# Tests
pytest>=8.0
